# Benchmark: Login Security

Use the PRD in `examples/prd-login.md` as the benchmark input.

Expected generation focus:

- Account/password login happy path.
- Wrong password and account enumeration prevention.
- Account lock after repeated failures.
- Redirect URL validation.
- Mobile form usability.
- Redis failure or lock expiry edge cases.
